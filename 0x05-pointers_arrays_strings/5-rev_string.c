#include "main.h"

/**
 * rev_string - starting point
 * @s: variable declaration
 * Return: always void
 */

void rev_string(char *s)
{
	int i = strlen(s) - 1;

        while (i >= 0)
        {
		i--;
        }
}
