#include "main.h"
/**
 * print_sign - Funtion declaration to print sign of a number
 * Return:0
 */
int print_sign(int n)
{
	int nom = 0;

	if (nom > 0)
		return (1);
	else if (nom == 0)
		return (0);
	else (nom < 0)
		return (-1);
}
