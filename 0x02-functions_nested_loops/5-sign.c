#include "main.h"
/**
 * print_sign - Funtion declaration to print sign of a number
 * Return:0
 */
int print_sign(int n)
{
	int n = 0;

	if (n > 0)
		return (1);
	else if (n == 0)
		return (0);
	else if(n < 0)
		return (-1);
}
