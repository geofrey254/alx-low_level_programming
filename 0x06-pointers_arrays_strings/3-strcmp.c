#include "main.h"

/**
 * _strcmp - starting point
 * @s1: s1 declared
 * @s2: s2 declared
 * Return: 0, <0, >0
 */

int _strcmp(char *s1, char *s2)
{
	if (s1 == s2)
		printf("Similar");
	else
		printf("Non-similar");

	return (0);
}
